package com.mycompany.userafncreator;
public enum EnumInstrMaq {
    EVAL, ADD, SUB, MUL, DIV, ASSIGN, BLTIN, PRINT, STOP, POP,
    JUMP, JUMP_TRUE, JUMP_FALSE, VARPUSH, CONSTPUSH,
    GT, LT, EQ, GE, LE, NE, AND, OR, NOT, 
    SET_AX, PUSH_AX,
    CALL, RET, PROCRET, ARGPUSH, ARGASSIGN
}